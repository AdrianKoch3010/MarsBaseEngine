#include <MBE/Map/TiledTerrainLayerComponent.h>

using namespace mbe;

TiledTerrainLayerComponent::TiledTerrainLayerComponent(EventManager & eventManager, Entity & parentEntity)
	: Component(eventManager, parentEntity)
{
}
